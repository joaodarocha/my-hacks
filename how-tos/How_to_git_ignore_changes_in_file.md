### Ignore file changes in `path/to/file`

```
git update-index --assume-unchanged path/to/file
```

### UnIgnore file changes in `path/to/file`e

```
git update-index --no-assume-unchanged path/to/file
```
